// 视口默认高度
export const DEFAULT_VIEWPORT_HEIGHT = 750

// 视口默认宽度
export const DEFAULT_VIEWPORT_WIDTH = 450

// 最小的视口长度
export const MIN_VIEWPORT_LENGTH = 300

// 默认插件 UI 头部高度
export const DEFAULT_UI_HEADER_HEIGHT = 50

// 默认插件的 UI 底部高度
export const DEFAULT_UI_FOOTER_HEIGHT = 50

// 默认 rem 转换基准字体大小
export const DEFAULT_BASE_FONT_SIZE = 16
